export default function Jobs () {
    return ( <div>
        <div>Search for a publishing job by title, job number, or the original submitter&apos;s username.</div>
        <h2>recent jobs</h2>
        <div>recent job listings</div>
    </div> );
}