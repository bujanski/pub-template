import styles from "./Header.module.css"

export default function Header() {
    return (
    <div id={styles.head}>
        <div id="app-title">
            publishing
        </div>

    </div>
        );
}
